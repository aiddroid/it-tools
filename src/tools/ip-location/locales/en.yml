tools:
  ip-location:
    title: IP Location
    description: Lookup your public IP(IPv4/IPv6) address(or a domain) where are you location.Find a geolocation of your IP adress including latitude, longitude, city, region and country.

    geolocationNote: The IP location uses several geolocation databases to locate the public IP address. But that does not mean that the IP location is accurate; that location is approximate.
    ipAddress: "The IP(IPv4/IPv6) address (or a domain, ex: google.com)"
    button:
      query: Query